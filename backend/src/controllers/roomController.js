function createRoom(req, res) {
  res.status(501).json({ message: 'Room creation not implemented yet' });
}

function listRooms(req, res) {
  res.status(501).json({ message: 'Room listing not implemented yet' });
}

module.exports = {
  createRoom,
  listRooms
};
